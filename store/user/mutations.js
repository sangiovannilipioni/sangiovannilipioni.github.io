export const setStrokeWidth = (state, strokeWidth) => {
  state.strokeWidth = strokeWidth
}

export const setAngle = (state, angle) => {
  state.angle = angle
}

export const setScale = (state, scale) => {
  state.scale = scale
}

export const setBlur = (state, blur) => {
  state.blur = blur
}
